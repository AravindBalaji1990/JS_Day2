// Regular function to add two numbers
function addRegular(a, b) {
    return a + b;
}

// Arrow function equivalent to the above
const addArrow = (a, b) => a + b;

console.log(addRegular(5, 3)); // Outputs 8
console.log(addArrow(5, 3));   // Outputs 8
